---
phase: 8
plan: 2
title: 导航状态 ↔ URL 同步
wave: 2
depends_on: ["08-01"]
files_modified:
  - web/src/App.tsx
  - web/src/hooks/useNavigationState.ts
requirements:
  - ROUTE-01
  - ROUTE-03
  - ROUTE-04
autonomous: true
---

# 08-02 Plan: Navigation State ↔ URL Sync

## Wave 2: Core routing bridge

### Task 1: Integrate useRouterSync into WorkspaceApp

<task id="08-02-T1">
<read_first>
- web/src/App.tsx
- web/src/hooks/useRouterSync.ts
- web/src/hooks/useNavigationState.ts
</read_first>
<action>
In App.tsx WorkspaceApp function:
1. Import useRouterSync
2. Call useRouterSync with setters: { setActiveSelection, setCurrentView, setSelectedTagIds, setSearchInput, setCalendarMode, setCalendarAnchor, setTimelineScale, setSelectedTaskId, setCalendarShowCompleted }
3. On activeSelection change: call navigateTo(activeSelection, push=true)
4. Replace direct setActiveSelection calls in nav actions with navigateTo

Also add a Routes/Route wrapper for the path-based navigation:
- Create a router wrapper component that reads path → initial activeSelection
- Route: /* → WorkspaceApp (single-page app pattern with catch-all)
</action>
<acceptance_criteria>
- Changing activeSelection updates URL path
- Navigating to /#/list/abc sets activeSelection to list:abc
- Build passes
</acceptance_criteria>
</task>

### Task 2: Update useNavigationState to accept URL-derived initial value

<task id="08-02-T2">
<read_first>
- web/src/hooks/useNavigationState.ts
- web/src/hooks/useRouterSync.ts
</read_first>
<action>
Modify useNavigationState to accept an optional `urlActiveSelection` parameter.
If provided (from URL parsing), use it as the initial value instead of localStorage.
Priority: URL > localStorage
</action>
<acceptance_criteria>
- URL-provided selection takes priority over localStorage
- Migration logic still runs for legacy values
- Build passes
</acceptance_criteria>
</task>

## must_haves
- URL changes on sidebar navigation
- Direct URL entry navigates to correct view
- Build passes
