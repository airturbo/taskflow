---
phase: 8
plan: 3
title: 过滤器/搜索/视图参数序列化
wave: 3
depends_on: ["08-02"]
files_modified:
  - web/src/hooks/useRouterSync.ts
  - web/src/App.tsx
  - web/src/hooks/useFilterState.ts
  - web/src/hooks/useViewConfig.ts
requirements:
  - ROUTE-02
  - ROUTE-03
autonomous: true
---

# 08-03 Plan: Filter/Search/View Query Params

## Wave 3: Full state serialization

### Task 1: Sync view config to query params

<task id="08-03-T1">
<read_first>
- web/src/App.tsx
- web/src/hooks/useRouterSync.ts
- web/src/hooks/useViewConfig.ts
</read_first>
<action>
In App.tsx, add effects to sync view config state to URL query params (replaceState):
- currentView → ?view=
- calendarMode → ?cal=
- calendarAnchor → ?anchor=
- timelineScale → ?scale=
- calendarShowCompleted → ?cal_done=1 (omit when false)

On mount/URL change: parse these from URL and set initial values.
Use replaceState (not pushState) — these are minor state changes, not navigation.
</action>
<acceptance_criteria>
- Switching view type updates ?view= in URL
- Refreshing page restores same view type
- Build passes
</acceptance_criteria>
</task>

### Task 2: Sync filter/search to query params

<task id="08-03-T2">
<read_first>
- web/src/App.tsx
- web/src/hooks/useRouterSync.ts
- web/src/hooks/useFilterState.ts
</read_first>
<action>
Sync filter/search state to URL:
- selectedTagIds → ?tags=id1,id2 (omit when empty)
- searchKeyword → ?q=keyword (omit when empty)
- selectedTaskId → ?task=taskId (omit when null)

On mount/URL change: parse and restore these from URL.
</action>
<acceptance_criteria>
- Active tag filters appear in URL
- Search query appears in URL as ?q=
- Selected task appears as ?task=
- Refreshing restores all filter state
- Build passes
</acceptance_criteria>
</task>

## must_haves
- All relevant state serialized to URL
- Refresh restores identical view (ROUTE-03)
- Build passes
