---
phase: 8
plan: 4
title: Back/Forward 导航 + 验证
wave: 4
depends_on: ["08-03"]
files_modified:
  - web/src/hooks/useRouterSync.ts
  - web/src/App.tsx
requirements:
  - ROUTE-04
autonomous: true
---

# 08-04 Plan: Back/Forward Navigation

## Wave 4: History navigation

### Task 1: Verify and fix popstate handling

<task id="08-04-T1">
<read_first>
- web/src/hooks/useRouterSync.ts
- web/src/App.tsx
</read_first>
<action>
React Router's HashRouter handles popstate automatically via useLocation.
Verify that the useEffect in useRouterSync that watches location properly:
1. Triggers on back/forward button
2. Updates all state (activeSelection, currentView, selectedTagIds, search, etc.)
3. Does NOT create circular update loops (URL → state → URL)

Add a `isNavigating` ref guard to prevent circular updates.
Test the full roundtrip: navigate → push to history → back button → state restored.
</action>
<acceptance_criteria>
- Back button navigates to previous selection
- Forward button navigates forward
- No infinite loops
- State fully restored on back/forward
- Build passes
</acceptance_criteria>
</task>

### Task 2: Handle initial redirect from /

<task id="08-04-T2">
<read_first>
- web/src/App.tsx
- web/src/main.tsx
</read_first>
<action>
Add a redirect from / to /focus in the route config.
Also handle the case where someone visits /#/ (empty hash).
Use React Router's Navigate component or redirect in routes.
</action>
<acceptance_criteria>
- / redirects to /focus
- Empty hash redirects to /focus  
- Build passes
</acceptance_criteria>
</task>

## must_haves
- Back/forward works correctly (ROUTE-04)
- No circular updates
- Build passes
- All 4 ROUTE requirements satisfied
