---
plan: "12-03"
title: "DND-03 — 桌面鼠标 + 移动触摸拖拽验证 + vite build 最终确认"
phase: "12"
status: "pending"
---

# Plan 12-03: 拖拽验证 + 最终 build

## 目标

验证 sensor 配置正确，touch-action 已设置，build 通过，并处理任何剩余的清理工作。

## Tasks

<task id="12-03-T1">
  <title>验证 touch-action 和 sensor 配置</title>
  <read_first>
    - web/src/components/views/KanbanView.module.css
    - web/src/components/WorkspaceShell.tsx
  </read_first>
  <action>
    确认以下配置：
    1. KanbanView.module.css 中 `.kanbanCard { touch-action: none }` 已存在
    2. WorkspaceShell 中 MouseSensor distance:6（与原 POINTER_DRAG_THRESHOLD=6 一致）
    3. WorkspaceShell 中 TouchSensor delay:200/tolerance:5（标准移动端配置）
    4. 检查 KanbanView 是否还有不必要的 workspace-helpers 工具函数引用可清理

    若需清理：移除 KanbanView 中不再使用的 import（POINTER_DRAG_THRESHOLD, buildPointerDragPreviewState 等）。
    这些在 12-01 时已清理，此 task 仅作确认。
  </action>
  <acceptance_criteria>
    - touch-action: none 在 .kanbanCard 上存在
    - sensor 配置数值正确
    - 无未使用的 import 警告（TypeScript noUnusedLocals）
    - vite build 通过
  </acceptance_criteria>
</task>
