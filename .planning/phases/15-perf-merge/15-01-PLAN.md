---
phase: 15
plan: 01
title: PERF-01 — KanbanDraggableCard + KanbanDroppableColumn React.memo
status: pending
---

# Plan 15-01: React.memo 渲染优化

## Objective
为 KanbanDraggableCard 和 KanbanDroppableColumn 添加 React.memo，减少不必要的重渲染。

## Tasks

<task id="15-01-A">
### A: Wrap KanbanDraggableCard with React.memo + custom comparator
- File: `web/src/components/views/KanbanView.tsx`
- Import `memo` from react
- Comparator: compare task.id, task.updatedAt, task.title, task.status, task.priority, task.completed, task.note, task.subtasks.length, selectedTaskId
- Rationale: lists/tags arrays change reference often; we only care about displayed fields
</task>

<task id="15-01-B">
### B: Wrap KanbanDroppableColumn with React.memo (default comparator)
- File: `web/src/components/views/KanbanView.tsx`
- Default shallow compare is sufficient (all primitive props + children)
</task>

<task id="15-01-C">
### C: Commit
- Message: `perf(kanban): React.memo on KanbanDraggableCard + KanbanDroppableColumn`
</task>
