---
phase: 15
plan: 04
title: SYNC-03 — field_versions JSONB column + domain type
status: pending
---

# Plan 15-04: field_versions Schema + Type

## Objective
在 Task domain type 添加 fieldVersions 字段，并创建 Supabase 迁移 SQL 文档。

## Tasks

<task id="15-04-A">
### A: Add fieldVersions to Task interface in domain.ts
- File: `packages/taskflow-core/src/domain.ts`
- Add: `fieldVersions?: Record<string, string>` // field → ISO timestamp of last write
- Place after `updatedAt`
</task>

<task id="15-04-B">
### B: Create migration SQL file
- File: `web/supabase/migrations/002_field_versions.sql`
- Document: current architecture embeds tasks in workspace_states.state_json JSONB, so no DDL change needed
- Include: commented-out ALTER TABLE for a future dedicated tasks table
- Include: explanation of field_versions semantics
</task>

<task id="15-04-C">
### C: Commit
- Message: `feat(sync): add fieldVersions to Task type + migration docs for field_versions`
</task>
