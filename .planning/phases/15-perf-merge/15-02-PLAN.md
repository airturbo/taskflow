---
phase: 15
plan: 02
title: PERF-02 — React.lazy 代码拆分 for all views
status: pending
---

# Plan 15-02: React.lazy 代码拆分

## Objective
将 WorkspaceViewContent.tsx 中所有 view 组件从 eager import 改为 React.lazy，实现按需加载。

## Tasks

<task id="15-02-A">
### A: Convert all view imports to React.lazy in WorkspaceViewContent.tsx
- Replace static imports with lazy dynamic imports
- Each named export wrapped: `lazy(() => import('./views/X').then(m => ({ default: m.X })))`
- Add `Suspense` import from react
- Add `lazy` import from react
</task>

<task id="15-02-B">
### B: Wrap each view render site with Suspense fallback
- Fallback: simple `<div className="view-loading-fallback" />` spinner or skeleton
- Place Suspense inside ViewErrorBoundary (so errors are caught but loading is smooth)
</task>

<task id="15-02-C">
### C: Add .view-loading-fallback CSS to App.css or shared CSS
- Simple centered spinner / opacity pulse animation
</task>

<task id="15-02-D">
### D: Commit
- Message: `perf(views): React.lazy code splitting for all desktop + mobile views`
</task>
