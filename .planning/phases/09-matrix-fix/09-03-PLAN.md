---
plan: "09-03"
title: "MATRIX-03 — 矩阵拖拽与移动端修正"
phase: "09"
status: "pending"
---

# Plan 09-03: 矩阵拖拽与移动端修正

## 目标

确保桌面端拖拽和移动端"移动到象限"功能都通过 isUrgent/isImportant 字段工作，不再依赖标签。

## Tasks

<task id="09-03-T1">
  <title>MobileMatrixView — onOpenInlineCreate 改用 isUrgent/isImportant</title>
  <read_first>
    - web/src/mobile/MobileMatrixView.tsx
  </read_first>
  <action>
    更新 MobileMatrixView 的 onOpenInlineCreate 调用，传 isUrgent/isImportant 而非 tagIds。
    无需修改 onMoveToQuadrant（已由 useTaskActions.moveTaskToQuadrant 处理）。
  </action>
  <acceptance_criteria>
    - 移动端创建任务传递正确的 isUrgent/isImportant
    - TypeScript 编译通过
  </acceptance_criteria>
</task>

<task id="09-03-T2">
  <title>TaskDetailPanel — 确保 isUrgent/isImportant 在编辑时可保存</title>
  <read_first>
    - web/src/components/TaskDetailPanel.tsx
  </read_first>
  <action>
    检查 TaskDetailPanel 是否有直接编辑 tagIds 包含特殊标签的逻辑。
    如有（如通过标签选择器添加 tag-urgent/tag-important），需要将其桥接到 isUrgent/isImportant 字段。
    如无，则确认无需修改。
  </action>
  <acceptance_criteria>
    - 详情面板编辑标签时不会重新引入特殊标签 tagIds
    - TypeScript 编译通过
  </acceptance_criteria>
</task>

<task id="09-03-T3">
  <title>useWorkspaceData — ensureSpecialTags 检查</title>
  <read_first>
    - web/src/hooks/useWorkspaceData.ts
  </read_first>
  <action>
    检查 useWorkspaceData 中 `ensureSpecialTags` 的用途（确保 tags 列表中有特殊标签）。
    由于特殊标签保留用于 UI 显示（TagManagementDialog），此调用保持不变。
    但需要确认 SYSTEM_TAG_IDS 过滤逻辑不会影响 isUrgent/isImportant 的展示。
  </action>
  <acceptance_criteria>
    - ensureSpecialTags 行为不变
    - 无副作用
  </acceptance_criteria>
</task>
