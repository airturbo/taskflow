---
phase: 13
task: 02
name: UX-03 重复任务卡片图标 + 完成 Toast 含下次日期
est: 1d
status: pending
---

# Plan 02 — UX-03: Recurring Task Icon + Next-Due Toast

## Goal
1. 所有视图的任务卡片：若有 repeatRule，显示 🔄 图标
2. 完成重复任务时，Toast/feedback 显示下次日期
   - Mobile: "已完成《title》 · 下次：MM-DD"
   - Desktop: actionToast 中添加下次日期信息

## Tasks

<task id="02.1">
ListView: add 🔄 icon in task-meta for recurring tasks
- Import describeRepeatRule from repeat-rule utils
- In .task-meta: {task.repeatRule && <span title={describeRepeatRule(task.repeatRule)}>🔄</span>}
</task>

<task id="02.2">
MatrixView: add 🔄 icon in task cards
- Find where task title/meta is rendered
- Add same recurring icon pattern
</task>

<task id="02.3">
KanbanView: add 🔄 icon in task cards
- Find task card meta area
- Add recurring icon
</task>

<task id="02.4">
Update MobileCompletionToast type + showCompletionToast call
- mobileUiStore: add nextDueLabel?: string to MobileCompletionToast type
- In App.tsx mobileToggleComplete: compute nextDueDateStr if task has repeatRule
  - Import nextDueDate from repeat-rule
  - Format: e.g. "4月15日"
- Pass nextDueLabel to showCompletionToast
</task>

<task id="02.5">
WorkspaceShell mobile toast: show next-due label
- In the mobileCompletionToast render section
- After title: {p.mobileCompletionToast.nextDueLabel && ` · 下次：${p.mobileCompletionToast.nextDueLabel}`}
</task>

<task id="02.6">
Desktop Undo Toast: show next-due for recurring tasks
- completionFeedback already has nextDueLabel (from Plan 01)
- WorkspaceShell: show it in the actionToast render
</task>

## Commit
`feat(ux): recurring task 🔄 icon + completion toast with next-due date (UX-03)`
