---
phase: 14
plan: 03
wave: 3
title: Minor UX — Attachments, Tag Color Preview, Timeline Today
reqs: [MINOR-01, MINOR-02, MINOR-03]
---

# Plan 03 — Minor UX Fixes

## Overview
- MINOR-01: Attachment upload limit 1.5MB → 10MB + progress bar
- MINOR-02: Tag color swatch hover live preview
- MINOR-03: Timeline week-scale today vertical line + day-scale auto-scroll

## Tasks

<task id="14-03-T1" name="Attachment 10MB + Progress Bar (MINOR-01)">
### Files to modify
- `web/src/utils/workspace-helpers.ts`
- `web/src/utils/app-helpers.ts`
- `web/src/components/TaskDetailPanel.tsx`

### Implementation
1. Change `MAX_EMBEDDED_ATTACHMENT_BYTES` to `10 * 1024 * 1024` in both utils files
2. In `TaskDetailPanel.tsx`:
   - Add `attachmentProgress: number | null` state (0-100 or null when idle)
   - Wrap `readFileAsDataUrl` calls with progress tracking using `FileReader.onprogress`
   - Show progress bar below attachment toolbar when `attachmentProgress !== null`
   - Progress bar: simple `<div>` with width percentage
3. Update error message strings to say "10MB" instead of "1MB" (Math.round result)
</task>

<task id="14-03-T2" name="Tag Color Hover Preview (MINOR-02)">
### Files to modify
- `web/src/components/TagManagementDialog.tsx`
- `web/src/components/TagManagementDialog.module.css`

### Implementation
1. Add `hoverColor: string | null` state in `TagManagementDialog`
2. In color swatch buttons: add `onMouseEnter={() => setHoverColor(color)}` + `onMouseLeave={() => setHoverColor(null)}`
3. Tag preview chip: use `hoverColor ?? newTagColor` for display
4. CSS: add `transition: background 0.1s, border-color 0.1s` to preview chip for smooth feedback
5. Same for tag edit rows in the list (existing tags being edited)
</task>

<task id="14-03-T3" name="Timeline Today Line + Auto-Scroll (MINOR-03)">
### Files to modify
- `web/src/components/views/TimelineView.tsx`
- `web/src/components/views/TimelineView.module.css`

### Implementation
1. **Week scale today column line**:
   - In week-scale grid, `scaleMarks` has `isToday` flag
   - Add a `.timelineWeekTodayLine` absolutely-positioned vertical line in the timeline lanes container
   - Calculate position: find today's column index among scaleMarks, compute `left: (index / total) * 100%`
   - Red/accent colored, 1-2px wide, semi-transparent
2. **Day scale auto-scroll to "now"**:
   - After mount, if `isDayScale && nowInWindow`, call `scrollIntoView` or set `scrollTop` on `.timelineDayScroll`
   - Target: scroll so the "now" line is ~30% from top of visible area
   - Use `useEffect([isDayScale, calendarAnchor])` with `scrollRef` pointing to `.timelineDayScroll`
3. CSS: `.timelineWeekTodayLine` styles
</task>

## Commit
`feat(phase-14): MINOR-01 attachment 10MB, MINOR-02 tag color preview, MINOR-03 timeline today`
