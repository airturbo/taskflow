---
phase: 14
plan: 02
wave: 2
title: Discoverability — Shortcut Tooltips + Guide Overlay + Bulk Ops
reqs: [DISC-01, DISC-02, DISC-03]
---

# Plan 02 — Discoverability

## Overview
- DISC-01: Add shortcut hint text to button tooltips
- DISC-02: First-use Top-5 shortcut guide overlay (localStorage-gated)
- DISC-03: Shift+Click bulk select in ListView + floating action bar

## Tasks

<task id="14-02-T1" name="Shortcut Tooltips (DISC-01)">
### Files to modify
- `web/src/components/WorkspaceShell.tsx`

### Implementation
1. Enhance view switcher button `title` attributes to include shortcut hint
   - "日历视图 (1)", "列表视图 (2)", etc.
2. Enhance Cmd+N / Cmd+K / ? buttons with shortcut in title
3. Already `formatShortcut` exists in `useGlobalShortcuts.ts` — use it to generate labels
4. No new component needed — native title tooltip is sufficient
</task>

<task id="14-02-T2" name="First-Use Shortcut Guide (DISC-02)">
### Files to create/modify
- `web/src/components/ShortcutGuideOverlay.tsx` (new)
- `web/src/components/ShortcutGuideOverlay.module.css` (new)
- `web/src/stores/desktopUiStore.ts`
- `web/src/App.tsx`

### Implementation
1. Add `shortcutGuideOpen: boolean` + `setShortcutGuideOpen` to `desktopUiStore`
2. In `App.tsx` useEffect: after 1.5s, check `localStorage.getItem('taskflow:shortcut-guide-seen')`; if null, set `shortcutGuideOpen = true`
3. `ShortcutGuideOverlay`: bottom-right corner card showing Top-5 shortcuts
   - ⌘N → 新建任务
   - ⌘K → 全局搜索
   - 1-5 → 切换视图
   - ? → 快捷键面板
   - Esc → 关闭弹层
4. "知道了" button → `localStorage.setItem('taskflow:shortcut-guide-seen', '1')` + close
5. "显示完整列表" button → open ShortcutPanel + close guide
6. CSS: slide-in from bottom-right, small card ~260px wide
</task>

<task id="14-02-T3" name="Bulk Ops: Shift+Click + Floating Bar (DISC-03)">
### Files to modify
- `web/src/components/views/ListView.tsx`
- `web/src/components/WorkspaceShell.tsx`
- `web/src/components/WorkspaceShell.module.css` (if exists)

### Implementation
1. **Shift+Click**: In ListView task item onClick, detect `event.shiftKey` → call `onToggleBulkSelect(task.id)` and enable bulk mode
2. **Floating action bar**: When `bulkSelectedIds.size > 0`, show floating bottom bar
   - Position: fixed bottom-center, above composer bar
   - Shows: "已选 N 条" + action buttons (完成, 移动, 删除)
   - Animated slide-up from bottom
3. Update bulk toolbar visibility: show in list view AND when bulkMode + any view has selection
4. Auto-enable bulkMode on Shift+Click
</task>

## Commit
`feat(phase-14): DISC-01/02/03 shortcut tooltips, guide overlay, bulk shift-click`
