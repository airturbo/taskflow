---
phase: 14
plan: 01
wave: 1
title: Kanban WIP Limit + Stats Action Buttons
reqs: [KANBAN-01, STATS-01]
---

# Plan 01 — Kanban WIP Limit + Stats Action Buttons

## Overview
- KANBAN-01: Add per-column WIP limits to KanbanView with visual warning when exceeded
- STATS-01: Add action buttons to Stats insight cards to jump to filtered views

## Tasks

<task id="14-01-T1" name="Kanban WIP Limit">
### Files to modify
- `web/src/components/views/KanbanView.tsx`
- `web/src/components/views/KanbanView.module.css`

### Implementation
1. Add WIP limit state in `KanbanView`: `wipLimits: Record<TaskStatus, number>`, default `{ todo: 0, doing: 5, done: 0 }` (0 = unlimited)
2. Add `editingWip: TaskStatus | null` state for inline WIP editing
3. `KanbanDroppableColumn` gets `wipLimit`, `count`, `isOverWip` props
4. Column header shows count badge: `<span className={styles.kanbanCount}>{count}</span>`
5. When `isOverWip`: add `styles.kanbanColumnOverWip` class to column header
6. Add small WIP indicator in header: "WIP: 5" with click to edit (inline input or simple prompt)
7. CSS: `.kanbanColumnOverWip` → red border-bottom + count badge red
</task>

<task id="14-01-T2" name="Stats Action Buttons">
### Files to modify
- `web/src/components/views/StatsView.tsx`
- `web/src/components/WorkspaceViewContent.tsx`
- `web/src/components/WorkspaceShell.tsx`

### Implementation
1. Add `onNavigate?: (view: string, preset?: { status?: string; due?: string }) => void` to StatsView props
2. Wrap "已逾期" stats card: clicking navigates to list view with overdue filter
3. Wrap "活跃任务" stats card: clicking navigates to list view (active tasks)
4. Wrap "已完成任务" stats card: clicking navigates to `system:completed` selection
5. Wrap "已排期" stats card: clicking navigates to timeline view
6. In `WorkspaceViewContent.tsx`: thread `onNavigate` prop
7. In `WorkspaceShell.tsx`: build `handleStatsNavigate` using `setCurrentView`, `setFilterStatus`, `setFilterDue`, `setActiveSelection`
</task>

## Commit
`feat(phase-14): KANBAN-01 WIP limits + STATS-01 action buttons`
