---
phase: 10
plan: 05
title: DATE-05 — 快速创建默认 dueAt + 可展开 deadlineAt
est: 0.5d
---

# Plan 05 — DATE-05: InlineCreatePopover dual-date fields

## Objective
在 InlineCreatePopover 中：
1. 将"日期"字段改为"计划完成"（maps to dueAt）
2. 添加可展开的"截止日期"字段（maps to deadlineAt）
   - 折叠时显示 "+ 添加截止日期" 按钮
   - 展开后显示 date + time 输入

## Tasks

<task id="05-1">
In InlineCreatePopover.tsx, rename "日期" label to "计划完成".
Add deadlineDateKey and deadlineTime to InlineCreateDraft type (or handle locally).
Add expandable deadlineAt section with toggle button.
</task>

<task id="05-2">
Update App.tsx inline create submit logic to map deadlineDateKey + deadlineTime to deadlineAt on task creation.
</task>

<task id="05-3">
Add CSS for the expandable deadline section in InlineCreatePopover.module.css.
</task>
