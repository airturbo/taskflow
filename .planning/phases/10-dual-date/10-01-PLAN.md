---
phase: 10
plan: 01
title: DATE-01 — TaskTimeSummary 双日期颜色/图标强化
est: 1.5d
---

# Plan 01 — DATE-01: TaskTimeSummary 双日期颜色/图标强化

## Objective
在 `shared.tsx` 的 `TaskTimeSummary` 组件中，同时清晰展示 dueAt（蓝色, 📅 计划）和 deadlineAt（红色, ⚡ 截止），让所有视图（ListView, KanbanView, MatrixView）自动受益。

## Tasks

<task id="01-1">
Update TaskTimeSummary in shared.tsx to show dueAt with blue styling and icon,
and deadlineAt with red styling and icon. Remove redundant formatTaskDualTimeSummary
fallback logic and replace with explicit dual-field display.
</task>

<task id="01-2">
Add CSS for .task-due-badge (blue) and .task-deadline-badge (red) in shared-components.css.
</task>

<task id="01-3">
Export a new helper formatDueAtBadge from selectors.ts that formats dueAt.
</task>
