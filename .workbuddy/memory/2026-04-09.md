# 2026-04-09 工作记录

## App 端数据持久化 Bug 修复验收通过
- 彻底废弃四层存储（SQLite + snapshot + Tauri store + localStorage 混用），统一改为纯 localStorage
- desktopMode = false，删除所有 desktop 专用 state，bundle 从 390KB 降至 335KB
- 体验官走查 11/11 PASS，master 确认 app 端问题已修复

## 产品上架路线图制定
- master 确认方向：以上架给其他用户使用为目标进行规划
- 制定了完整的 5 阶段路线图：后端账户 → 多端同步 → 功能补齐 → 分发合规 → 商业化
- 存档路径：`brain/a70822209d774cab811eefb4e6aa7aae/product-launch-roadmap.md`
- master 决策：先用 Supabase，快速验证 PMF 后再评估自建

## Phase 1 Supabase 账户体系 完成
- 安装 @supabase/supabase-js，构建 web ✅ 桌面端 ✅，bundle 348KB
- 新建文件：
  - `src/utils/supabase.ts` — 客户端初始化，未配置时自动 null（访客模式）
  - `src/types/supabase.ts` — Database 类型定义（profiles / workspace_states / sync_cursors）
  - `src/hooks/useAuth.ts` — 认证 hook（signIn/signUp/signOut/Google OAuth/resetPassword）
  - `src/components/AuthPage.tsx` — 登录/注册页（Tab 切换、Google OAuth、访客模式入口）
  - `src/components/AuthGate.tsx` — 路由守卫（loading → auth → migration → ready）
  - `src/components/MigrationWizard.tsx` — 旧版本地数据迁移向导
  - `supabase/migrations/001_init.sql` — 数据库初始化 SQL（含 RLS 策略）
  - `.env.local.example` — 环境变量配置模板
- 改造 `src/utils/storage.ts`：离线优先（先写 localStorage，400ms 节流后异步 upsert Supabase）
- `src/main.tsx` 包裹 `<AuthGate>`；`WorkspaceApp` 顶栏加用户头像/退出按钮
- 关键设计：Supabase 未配置时全程访客模式，不影响现有 demo 体验

## Supabase 项目配置完成
- Project URL：https://yprcnagrqifswmhuxpgy.supabase.co
- .env.local 已写入（不进 git），dev server 在 :1421 运行
- 数据库 001_init.sql 已由 master 在 Dashboard 执行（profiles / workspace_states / sync_cursors 三表 + RLS）

## Phase 2+3 完成（web ✅ 桌面端 ✅，bundle 556KB）
- Phase 2 新增文件：
  - `src/hooks/useRealtimeSync.ts` — Supabase Realtime 订阅 + newer-wins 合并 + 断线重连 + forceSync
  - `src/utils/offline-queue.ts` — 离线队列（断网攒操作，上线 flush）
  - `src/components/SyncIndicator.tsx` — 顶栏同步状态圆点指示器（idle/syncing/synced/offline/error）
- Phase 3 新增文件：
  - `src/utils/repeat-rule.ts` — 重复规则工具（daily/weekdays/weekly/monthly/yearly/custom:Nd/Nw/Nm）
  - `src/hooks/useGlobalShortcuts.ts` — 全局快捷键（⌘N 新建、⌘K 搜索、Esc 取消、1-5 切视图）
- App.tsx 改动：toggleTaskComplete 接入重复任务生成、顶栏加 SyncIndicator、快速创建/搜索 ref 绑定、快捷键注册

## 代码推送 GitHub + 品牌名确认
- 产品正式命名为 TaskFlow，GitHub 仓库：https://github.com/airturbo/taskflow.git
- Bundle identifier：com.airturbo.taskflow
- 代码已推送 main 分支（2 commits），keychain 已配置，后续 git push 无需再输 token
- 品牌名替换：FlowTask → TaskFlow，storage key：taskflow-v2，device key：taskflow-device-id

## 5项功能迭代完成（体验官 9.4/10 PASS）
- 功能1：**系统主题跟随** — `useSystemTheme` hook，三档循环（💻跟随系统/🌙深色/☀️浅色），新用户默认 'system'
- 功能2：**自然语言增强** — smart-entry.ts 重写，支持下周X/下月N号/N天后/N周后/#标签/!优先级
- 功能3：**统计页升级** — 30天完成趋势柱状图 + Streak（连续完成天数）+ 专注时长累计
- 功能4：**快捷键面板** — ShortcutPanel 组件，? 键呼出，4个分组展示所有快捷键
- 功能5：**批量操作** — 列表视图多选 checkbox + 批量完成/移动/打标签/删除
- 构建通过 ✅，已推送 GitHub main（commit: 1dd603b），Vercel 自动部署中

## 响应式设计系统完成（commit: 6052e67）
对标 Things 3 / Todoist Mobile，完整三档断点体系：
- **移动端（≤720px）**：精致顶部标题栏（品牌名+工具图标）、底部5栏标签导航、看板/四象限单列垂直、日历自动降级Agenda、创建弹层改为底部Sheet、快速创建栏精简为单行、触摸目标≥44px
- **抽屉系统**：入场/出场动画（translateX，0.26s cubic-bezier）、背景滚动锁定、左/右方向支持（导航左滑入）、关闭按钮升级为圆形图标按钮
- **安全区适配**：iOS刘海屏/全面屏 env(safe-area-inset) + PWA display-mode:standalone
- CSS新增约350行，修复 task-card.is-selected 漏括号遗留bug

## 清单与文件夹管理功能完成（commit: d532853）
- 新增 CRUD：createFolder / renameFolder / updateFolderColor / deleteFolder / createList / renameList / updateListColor / updateListFolder / deleteList
- 安全删除：删除文件夹时其清单移到顶层；删除清单时其任务移到收件箱
- 侧边栏 UI：hover 显示 ✎ 重命名 / ✕ 删除按钮，点色点循环切换10色预设
- 内联重命名：点击 ✎ 或直接右键 → 行内 input，Enter 确认 / Esc 取消
- 右键上下文菜单：folder（新建清单/重命名/删除）、list（重命名/移动文件夹/删除）
- 同步修复 git committer 信息（airturbo），解决 Vercel Hobby 部署 Blocked 问题

## Vercel 部署修复（commit: 01bede5）
- 根因：vite.config.ts 里 `build.target` 依赖 TAURI_ENV_PLATFORM 环境变量，Vercel 环境下为 undefined 导致 target 异常
- 修复：新增 `vercel.json`（buildCommand: npm run build:web，outputDirectory: dist）+ package.json 加 `build:web` 脚本（强制清空 TAURI_ENV_PLATFORM）
- 仓库已改为 Public，Hobby 套餐限制不再触发

## 滚动条隐藏 + 响应式四档重构（commit: 7012570）
- 滚动条：全局 scrollbar-width:none + webkit 隐藏，彻底无滚动条（Things 3 / Todoist 风格）
- 断点体系重构为四档：>1280（全三栏）/ 960-1200（折叠侧边栏48px图标）/ 680-960（导航变抽屉）/ <680（手机底部导航）
- 折叠侧边栏：宽度48px只显示图标，hover时展开256px并覆盖主内容区（Linear风格），0.2s cubic-bezier动画
- NavButton 加 icon prop，系统视图全部配语义图标（◎全部/☀今日/↗未来7天/📥收件箱/✓已完成/🗑回收站/📊统计）
- App 已更新安装到 /Applications/TaskFlow.app

## CRAP 原则注入角色 + 布局修复（commit: 15a4938）
- roles.v1.yaml：PM/UI设计师/前端开发 system_prompt 注入 CRAP/Gestalt/44px/层级/克制原则
- 折叠侧边栏重构：改为 position:fixed 展开（脱离文档流，不崩溃主内容区）+ 遮罩 + 点击 toggle 控制（不再用 hover）
- 侧边栏排版重建：分组间距 20px（CRAP Proximity）、标签 10px uppercase 700（Contrast）、4px 基础网格
- 抽屉修复：z-index 提升到 150，backdrop 全屏覆盖，left 抽屉宽度改为 min(300px,88vw) 并正确靠左

## 侧边栏四列网格对齐（commit: 703e2d0）
- NavButton 重建为 CSS Grid 四列：[icon 20px][label 1fr][mode-pills auto][count 28px]，所有行图标/数字垂直对齐
- 模式切换从 nav-button__mode-switcher 重命名为 nav-button__mode-pills + mode-pill class，视觉改为轻量 pill
- 收件箱（list:inbox）改为 folder-list-item 样式，与普通清单同级左对齐，不再居中
- 智能清单 filter.icon 单独传给 icon prop，不再与 label 拼接，对齐到图标列
- folder-list-item 也改为相同四列网格，文件夹内子清单图标列宽 32px（含12px缩进体现层级）
- 折叠侧边栏 compact 模式：nav-button 改为单列 36px 居中图标，展开态恢复四列

## 折叠侧边栏展开布局修复（commit: 5f02ec0）
- 根因：sidebar--compact.is-expanded 是 position:fixed，但 app-shell grid 第一列 48px 仍占位，主内容区被压缩
- 修复：app-shell 加 has-expanded-sidebar class，触发 grid-template-columns: 0 1fr，document flow 第一列收为 0
- sidebar--compact.is-expanded 时 overflow:hidden + padding:0，防止 width:0 内容溢出
- 展开时恢复隐藏内容补全 sidebar-section > sidebar-stack > * 规则

## 记忆沉淀完成（2026-04-09 EOD）
- MEMORY.md 全面更新：项目基本信息/用户画像/技术基线/设计原则/历史决策/工作流
- `.agent-team/configs/USER_PROFILE.md` 新建：角色团队专用用户画像，含沟通/审美/决策/Bug 处理规范
- `roles.v1.yaml` 顶部加 user_profile_ref，所有角色启动时必读
- 全局 `~/.workbuddy/USER.md` 更新，沉淀 master 深度画像

## 文件夹层级 + 收件箱图标 + 抽屉修复（commit: 94c52f4）
- 文件夹标题（父级）：font-weight 700 + color:text + 9×9px 方形圆角色点，明显区别于子清单
- 子清单：font-size 13px + color:text-secondary + 7px 圆形色点 + padding-left:12px 缩进
- 收件箱：换 📥 emoji 图标，不再是普通蓝色圆点，通过 .folder-list-item__inbox-icon 渲染
- 模块标题改为「文件夹与清单」，按钮顺序先「＋文件夹」后「＋清单」
- 左侧导航抽屉宽度改为 min(280px,90vw)，补充 overflow-x:hidden，sidebar--drawer 清除重复定义

## 任务详情分级交互（commit: c1dd2cb）
- selectTask() 统一函数：手机→底部Sheet，平板/中屏→右侧400px抽屉自动滑出，宽屏→固定rail无需操作
- TaskBottomSheet 组件：92vh最大高度，20px圆角，拖动把手，下滑80px关闭，毛玻璃backdrop，安全区适配
- ResponsiveDrawer 加 width prop，详情抽屉默认宽度改为 min(400px,92vw)
- 所有视图 onSelectTask 统一改为 selectTask（5处），openProjectionTaskDetail/jumpToProjectionTask 也走统一逻辑
