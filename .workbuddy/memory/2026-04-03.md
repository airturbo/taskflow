# 2026-04-03

- 完成 align-acceptance：冻结本轮 3 个 P2 收口边界与团队治理边界。产品侧决定保持搜索为当前工作区即时过滤、仅对查询态做轻量 debounce；日历只把显式创建入口纳入正式验收，整格/整列点击创建应收紧；列表/看板/四象限卡片根节点必须补键盘焦点与 Enter/Space。治理侧决定体验官先快速理解 PRD / scope / 主链路，再以独立挑剔用户身份终审；后续 runtime 采用项目本地 roles/config 优先、全局回退的立即生效策略。
- 完成本轮治理闭环：重新构建 `web/` 并跑通 `.workbuddy/browser-audit/ux-review-runner.mjs`，验证搜索 debounce、日历显式创建边界与列表/看板/四象限卡片键盘语义全部通过；同时已创建 `.agent-team/roles/roles.v1.yaml`，同步本地 `router-config/state-machine/ux-governance-rules/ux-review-sop/project-state`，新增 `ART-UX_REVIEW_REPORT-0003 v1.1.0`、`ART-UX_ISSUE_LOG-0003 v1.1.0` 与 `ART-EAN-0002 v1.0.0`，把当前项目状态推进到 `release_preparation`。
- 完成新一轮 web / desktop parity 深度复审：修正 `web/src/App.tsx` 与 `web/src/index.css`，解决网页端全屏右侧详情栏横向滚动；扩展 `.workbuddy/browser-audit/ux-review-runner.mjs` 加入长附件名 / 长评论压力用例与横向溢出测量，复测得到 `detail/rightRail/page hasHorizontalOverflow = false`；同时跑通 `npm run desktop:build` 生成 `Todo Workspace.app`，并新增 `CR-20260403-001` / `IA-20260403-001`、`ART-UX_COVERAGE_MATRIX-0003 v1.0.0`、`ART-UX_REVIEW_REPORT-0004 v1.0.0`、`ART-UX_ISSUE_LOG-0004 v1.0.0`、`ART-EAN-0003 v1.0.0` 和 `docs/pm/2026-04-03-round13-22-ux-iteration-summary.md`，把 10 轮体验循环和 app/web parity 结论正式落盘。
- 输出当前会话结构化总结，并确认本轮后续最高优先级已切换为：修复 app 端跨视图任务数量不一致严重 bug、从技术/产品双视角复核任务存储与更新机制，以及给出提醒系统竞品化优化方案；当前状态仍处于根因定位与方案梳理，尚未开始对这些新问题落代码。
- 完成跨视图一致性主问题收口：更新 `web/src/App.tsx`，让 desktop 列表 / 看板 / 四象限 / 时间线 / 日历直接使用 repository 查询结果作为主渲染数据源，同时把侧边栏计数补齐到 `selectedTagIds + searchKeyword` 当前工作区过滤；抽出统一的 `getCalendarTaskAnchor/getCalendarTaskDateKey`，收口 calendar month/week/agenda 挂日规则；扩展 `.workbuddy/browser-audit/ux-review-runner.mjs` 增加跨视图数量/标题 parity 回归并修复脚本稳健性，最新 `ux-review-summary.json` 中 `allMatchesList/listMatchesKanban/listMatchesMatrix/weekMatchesAgenda` 全部为 `true`；另新增 `docs/pm/2026-04-03-task-consistency-and-reminder-review.md`，正式记录任务存储/更新机制复核结论与提醒系统竞品化优化方案。
- 根据用户新截图继续二次收口“列表 5 条但‘全部’/‘活跃任务’只有 3 条”问题：在 `web/src/App.tsx` 新增 `activeSelectionKey` 与 `buildTaskStats`，让列表/看板/四象限这类完整工作区视图的活动 sidebar count 与顶部 hero 指标直接对齐当前已渲染任务集合，而不再单独等待 repository 摘要回流；同时更新 `.workbuddy/browser-audit/ux-review-runner.mjs`，新增 `activeMetricCount`、`allMatchesActiveMetric`、`activeMetricMatchesList` 三项回归，最新结果已为 `true`，并把结论补记到 `docs/pm/2026-04-03-task-consistency-and-reminder-review.md`。
- 启动新手引导方案讨论前，已先盘点当前产品与数据基线：`web/src/App.tsx` 现已覆盖列表 / 日历（月周agenda）/ 看板 / 时间线（日周）/ 四象限 / 统计、顶部快速创建、右侧详情、提醒中心与多处内联创建；现有引导仅有 `EmptyState` / `SparseGuide` / 内联 guidance 与提醒权限提示，尚无完整 onboarding；默认示例数据唯一真源仍是 `web/src/data/seed.ts`，并通过 `web/src/utils/storage.ts` 在 web/localStorage 与 desktop SQLite repository 初始化时复用。这意味着后续实现应优先做“轻量但高级的分阶段 onboarding + 与引导剧本配套的 seed 数据重写”，而不是再单独堆一个帮助页。
- 已完成新手引导团队讨论稿并落盘到 `docs/pm/2026-04-03-onboarding-discussion-and-implementation-plan.md`：正式确定首版采用“欢迎层 + 示例工作区 + 可恢复 checklist”的半引导方案，要求不引第三方 tour、改为在现有持久化链路中新增 onboarding state，自建轻量 welcome/checklist/context cue 组件，并把 seed 数据从功能样本重写为带剧情的 onboarding 剧本；默认体验目标是 3 分钟内让新用户亲手完成创建、安排、拖拽、详情补全、完成/专注这 5 类关键动作。

