# 2026-04-02

- 桌面端 SQLite 第一阶段已落地：新增 `web/src/utils/desktop-sqlite.ts`，通过 Tauri SQL 插件把持久化从 Store + JSON 迁移到 `sqlite:todo-workspace.db` 的 snapshot 表，并保留旧 Store / localStorage 迁移与兜底；`npm run build`、`npm run desktop:build`、`npm run desktop:dev` 复测通过，当前下一步应继续把 snapshot 演进为结构化表与 repository。
- 已完成一轮工作台交互收口：`web/src/App.tsx` 与 `web/src/index.css` 新增响应式抽屉骨架，小屏优先保主工作区；修复日历（月/周/agenda）与看板拖拽的 payload 落点识别；四象限改由“紧急 / 重要”特殊标签驱动并支持拖拽改标签；标签筛选改成多选交集语义，`npm run build` 已通过。
- 桌面端 SQLite 第二阶段已落地：新增 `web/src/utils/desktop-repository.ts` 作为 repository 层，在 `web/src/utils/desktop-sqlite.ts` 中补齐 v2 结构化表迁移（workspace_state / folders / lists / tags / filters / tasks），并把 `web/src/utils/storage.ts` 的桌面主链路切到“repository 优先 + snapshot / Store / localStorage 迁移兜底”；`npm run build` 与 `npm run desktop:build` 已复测通过。
- 桌面端 SQLite 第三阶段已落地：`web/src/utils/desktop-sqlite.ts` 新增 v3 migration，把旧 `tasks` 表里的 `tagIds / reminders / subtasks` JSON 镜像回填到 `task_tags / task_reminders / task_subtasks` 子表；`web/src/utils/desktop-repository.ts` 现在会优先从这些子表 hydrate 任务，并暴露按 taskId / listId / status / 完成态 / 到期时间 / 标签交集查询的 repository 入口；`npm run build` 与 `npm run desktop:build` 已复测通过。
- 桌面端 SQLite 第四阶段已落地：`web/src/utils/desktop-sqlite.ts` 新增 v4 migration，把旧 `tasks` 表里的 `attachments / collaborators / comments / activity` JSON 镜像回填到 `task_attachments / task_collaborators / task_comments / task_activity` 子表；`web/src/utils/desktop-repository.ts` 已升级为优先从全部任务关系表 hydrate，并在保存时同步写回这些结构化记录；`npm run build` 与 `npm run desktop:build` 已复测通过。
- 盘点了 `.agent-team` 治理现状：当前权威基线已是 `BL-20260331-003`，但 `configs/global/project-state.v1.json` 与长期记忆仍滞后到 `BL-20260331-002`，且 `logs/audit/audit-log.v1.jsonl` 未补入 `CR-20260401-001 / IA-20260401-001`；已向主代理建议本轮桌面数据收口至少补 `CR-20260402-001`、`IA-20260402-001`、`ARCHITECTURE_DOC--ART-ARCH-0001--v3.0.0.md`、`TECH_DECISION_RECORD--ART-TECH_DECISION_RECORD-0001--v1.0.0.md`、`TEST_REPORT--ART-TEST-0001--v3.0.0.md` 后再冻结新基线。
- 已完成 phase5 查询侧盘点：当前桌面端提醒调度、标签筛选、统计计数仍主要依赖 `web/src/App.tsx` 与 `web/src/utils/reminder-engine.ts` 在全量 `tasks` 数组上做内存过滤；repository 虽已支持 `list/status/completed/due/tagIds` 基础查询，但仍缺少 selection DSL、提醒候选查询、aggregate/count、搜索、calendar/timeline 时间窗与轻量 projection，这些应作为下一阶段读路径下推的优先项。（2026-04-02）
- 已完成 phase5 保存侧盘点：当前桌面端保存仍由 `web/src/App.tsx` 的整份 `PersistedState` effect 驱动，经过 `web/src/utils/storage.ts` 固定落到 `saveDesktopRepositoryState`，而 `web/src/utils/desktop-repository.ts` 仍会整表删除并重建 `folders / lists / tags / filters / tasks / task_*`；下一阶段最优切口是先拆 `workspace_state` 独立保存，再在 repository 层落单 task upsert + 单 task relations replace，并把 snapshot / legacy store 降为节流 checkpoint，同时处理 repository 与 snapshot 优先级、JSON 镜像双写和 sort_order 一致性风险。（2026-04-02）
- 桌面端数据层 phase5 已落地：`web/src/utils/desktop-repository.ts` 扩展了 selection DSL、关键词、提醒候选、标签交集与 aggregate/count 查询，`web/src/App.tsx` 在桌面模式下把提醒、工作区任务筛选、导航计数与统计优先切到 repository 直查，`web/src/utils/storage.ts` 新增桌面持久化串行队列；同时 `saveDesktopRepositoryState()` 改为按 task / relation 增量 upsert，不再每次整库清空重写；本轮还补了 `CR-20260402-001`、`IA-20260402-001`、`ART-TEST-0002`、`ART-DELIVERY-0002` 与 audit 记录，`npm run build`、`npm run desktop:build` 均复测通过。
- 追加完成 phase5 最终收口：统计页剩余的 priority/tag 分布也改成 repository aggregate，新增 `ARCHITECTURE_DOC--ART-ARCH-0001--v3.0.0.md`、`TECH_DECISION_RECORD--ART-TECH_DECISION_RECORD-0001--v1.0.0.md`、`TEST_REPORT--ART-TEST-0002--v1.1.0.md`、`DELIVERY_NOTE--ART-DELIVERY-0002--v1.1.0.md`，并冻结新基线 `BL-20260402-001`；`project-state.v1.json`、`artifact-registry.v1.json`、`baseline.history.v1.jsonl` 与 audit log 已同步更新，构建再次通过。
- 已把桌面主工作台继续推进成更真实的 query-first task shell：`storage.ts` 冷启动优先读取 workspace shell 元数据而不再整包装载全部 tasks，`App.tsx` 在桌面模式下按当前视图拉取 selection query，日历改为按月/周时间窗查询，时间线改为按 day/week 窗口 overlap 查询，并新增 `desktopTimelineTasks` / `desktopSelectedTask` 状态；任务编辑、拖拽改期、提醒/子任务/评论/附件等操作改为单 task 增量持久化，`desktop-repository.ts` 补了 calendar/timeline window、offset/limit、sortBy 能力，`desktop-sqlite.ts` 新增 `start_at` 与 `deleted+completed+time` 组合索引，`npm run build` 与 `npm run desktop:build` 已通过。
- 继续收口桌面端 fallback 持久化：`web/src/utils/storage.ts` 已把 snapshot / legacy store 镜像从 420ms 高频防抖改为“6 秒 idle / 最长 30 秒”低频 checkpoint，避免每次 workspace shell 或单 task 保存后都立刻整包回写 fallback；当前主数据源仍是 SQLite repository，兼容镜像只保留为恢复抓手。
- 已再次确认交付形态不是“只有网页预览”：当前仓库同时维护 Web 预览与 Tauri 桌面壳，`web/src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`src-tauri/src/lib.rs` 及 `package.json` 的 `desktop:dev / desktop:build` 均在位，且 `App.tsx` / `storage.ts` 的桌面模式逻辑已接入最新 query-first / SQLite repository 改动；macOS bundle 产物位于 `web/src-tauri/target/release/bundle/macos/Todo Workspace.app`。
- 已确认当前分享路径：仓库现阶段可直接产出 macOS `.app` 供内测分发，但 `tauri.conf.json` 仅配置 `bundle.targets = ["app"]`，尚未看到签名、公证、自动更新或正式发布配置；若要给更多人稳定使用，应补签名/公证与更顺手的分发包装（如 dmg）。
- 已定位当前“macOS App 与网页预览不一致”至少有三层原因：其一，用户当前打开的 `web/src-tauri/target/release/bundle/macos/Todo Workspace.app` 时间戳为 17:41，已落后于 18:03 更新的 `web/src/utils/storage.ts`；其二，桌面端走 Tauri SQLite repository / snapshot / store，而网页继续走 browser localStorage，首次迁移后不会自动双向同步；其三，`App.tsx` 与 `desktop-repository.ts` 里桌面分支本就采用 query-first + SQL 查询，和网页内存过滤在分页、排序、统计口径、冷启动壳状态上存在行为差异。
- 已先完成一轮桌面交互一致性修复并重新打包：`web/src/App.tsx` 把桌面主视图渲染统一切回基于前端任务缓存的 `fallbackVisibleTasks / fallbackCalendarTasks`，不再优先吃滞后的 `desktopVisibleTasks / desktopCalendarTasks / desktopTimelineTasks`，从而让拖拽、复制、恢复、改期等操作即时回显并更接近网页预览；时间线拖动链路也从 mouse 事件改为 pointer 事件，`web/src/index.css` 额外补了 `user-select: none`、`-webkit-user-drag: element` 与 timeline `touch-action: none` 以收口 macOS WebKit 壳里的拖拽兼容性；新的 `.app` 已于 19:14 重建通过，后续若看板/日历/四象限原生 DnD 仍偶发不稳，下一步应整体迁到 pointer 驱动自定义拖拽。
- 已完成第二轮桌面拖拽修复：`web/src/App.tsx` 里的看板 / 日历 / 四象限已全部脱离原生 HTML5 drag-and-drop，统一改成 `onPointerDown + pointermove/up/cancel + elementFromPoint + data-* drop zone` 的自定义拖拽命中链路，并加了拖动阈值与 click 抑制，避免桌面端 WebKit 壳里 100% 拖不动；`web/src/index.css` 同步移除 `-webkit-user-drag: element`，改为统一 `touch-action: none` 与 grab/grabbing 反馈；`npm run desktop:build` 于 19:27 再次通过，最新包为 `web/src-tauri/target/release/bundle/macos/Todo Workspace.app`。
