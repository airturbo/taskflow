# 2026-04-07

- 已把新手引导第一版正式落到代码：`web/src/App.tsx` 新增欢迎层、可恢复 checklist、anchored cue 与自动完成逻辑，`web/src/types/domain.ts` / `utils/storage.ts` / `utils/desktop-repository.ts` / `utils/desktop-sqlite.ts` 为 `onboarding` 状态补齐跨 web 与 desktop 的持久化链路；同时 `web/src/data/seed.ts` 重写为 onboarding 剧本化示例工作区，并已通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build`。
- 复查发现当前 onboarding 触发条件过严：`web/src/App.tsx` 的 `isOnboardingSeedState()` 通过“folder/list/task id 与数量完全等于 seed signature”判断是否展示欢迎层，因此只要浏览器 localStorage(`ticktick-parity-demo-v2`) 或桌面端 workspace shell / snapshot 已有旧数据，欢迎层就不会出现；更严重的是用户完成第一步“新建任务”后任务总数变化，也会立刻失去 onboarding 资格，导致 checklist / 恢复入口一起消失。另确认标签系统当前只支持在右侧详情的“完成与标签”区域勾选已有标签，不支持创建/重命名/删除标签，也没有新建任务时的标签入口。
- 已完成本轮修复：`web/src/App.tsx` 把 onboarding 判定改为“`seedScenarioVersion === v1` + onboarding 工作区骨架存在”，并在 `utils/storage.ts` / `utils/desktop-repository.ts` / `utils/desktop-sqlite.ts` 把历史工作区 fallback 与 migration 默认值收口到 `legacy`；同时新增统一标签选择器与标签管理弹层，补齐侧边栏管理入口、顶部快速创建标签、内联创建标签、详情编辑标签，以及普通标签的新建/重命名/删除能力；构建已再次通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build`。
- 已完成 onboarding 第三轮收口的关键缺口：`web/src/App.tsx` 新增“体验欢迎层”动态入口，允许已有数据用户无损导入 onboarding 示例工作区并重新打开 welcome brief；同时把欢迎层的“先随便逛逛”从强制跳第一步改为轻量浏览路径，构建已再次通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build`。
- 已继续收口 onboarding 体验：`web/src/App.tsx` 把当前引导步骤改为优先跟随 `onboarding.currentStepId`，修正 checklist / cue 总跳回首个未完成步骤的混乱行为；同时把欢迎层、右侧提醒/详情栏和活动时间文案从英文占位与 locale 随机格式收口成稳定中文产品语义。另已同步增强 `.agent-team/roles/roles.v1.yaml` 与 `configs/global/{ux-governance-rules,ux-review-sop,ux-review-templates}.v1.md`，要求体验官必须进入前台真实交互并保留可回放证据，不能再只读文档和代码就宣称“已完整体验”。
- 已按新体验官规则重新做前台 focused 走查，并把结论正式提交给项目团队：证据位于 `.workbuddy/browser-audit/results/2026-04-07-ux-officer-review/summary.json` 与 `screenshots/01-05`；新建 `CR-20260407-001` / `IA-20260407-001`、`ART-UX_COVERAGE_MATRIX-0004`、`ART-UX_REVIEW_REPORT-0005`、`ART-UX_ISSUE_LOG-0005`，确认当前需返工的 4 个问题为 onboarding 第 1 步带时间创建会跳过排期步骤、checklist 直跳“完成一条任务”时主区/右栏上下文错位、“先看示例工作区”默认折叠且失去提示，以及浏览器端 AudioContext warning 暗示提醒/番茄声音回退不够稳；项目状态已从 `release_preparation` 回到 `impact_assessment`。
- 已补记本轮治理角色边界：体验官只负责以挑剔用户视角提出问题与最终复验，不负责具体修复和团队管理；产品经理负责承接体验官问题、优化产品逻辑，并与项目经理协调团队落地，完成后再交体验官复验。
- 已把这套角色运转方式正式沉淀进 agent team skill：同步更新了全局 `~/.workbuddy/skills/multi-role-project-team/` 的 `SKILL.md`、`references/{roles,ux-governance-rules,ux-review-sop,ux-review-templates,state-machine}`，并同步到当前项目本地 `.agent-team/roles/roles.v1.yaml` 与 `configs/global/{ux-governance-rules,ux-review-sop,ux-review-templates,state-machine}.v1.*`；后续体验问题默认按“体验官提问题 → 产品经理承接并改产品逻辑/验收 → 项目经理协调落地 → 体验官复验”执行。
- 已按新机制把 `UX-017/018/019/020` 转成产品经理承接单并连续跑完第 23-32 轮 focused 闭环：`web/src/App.tsx` 收紧 onboarding 第 1/2 步完成语义、修正第 5 步主区/右栏上下文并让 browse path 保留 checklist + cue，`web/src/hooks/useReminderCenter.ts` 改成首个真实用户手势后再解锁 AudioContext；随后新增 `.workbuddy/browser-audit/ux-review-round23-32.mjs` 与证据目录 `.workbuddy/browser-audit/results/2026-04-07-round23-32-ux-review/`，复查确认 4 个问题全部关闭，并补齐 `docs/pm/2026-04-07-round23-32-ux-iteration-summary.md` 及 `ART-UX_{COVERAGE_MATRIX,REVIEW_REPORT,ISSUE_LOG}-0004/0005 v1.1.0` focused 制品。
- 已继续修复 onboarding 场景下右侧详情栏展示错乱：`web/src/App.tsx` 将顶部 `清单/优先级/开始/截止/完成/标签` 区块重新编排为稳定纵向层级，并把“完成状态”与“标签”拆成独立 section；`web/src/index.css` 同步把 detail grid 显式改成单列、允许标签头部换行，避免原生 `datetime-local` 与标签管理按钮在窄栏中互相挤压。构建已再次通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build`。
- 已补查当前系统视图与多视图筛选语义：`system:today` 仅按 `dueAt` 命中“今天或已逾期”，`system:upcoming` 仅按 `dueAt` 命中“今天起 7 天内”且不含逾期；而“系统视图 → 全部”进入日历/时间线后并不等于展示全部任务，这两类视图会在基础 selection 之外继续叠加标签交集、搜索词，以及各自的排期投影规则——日历只展示当前月/周窗口内且有 `startAt`/`dueAt` 锚点的任务，时间线只展示与当前日/周时间窗相交的已排期任务；无排期任务天然不会进入这两个视图。
- 已形成一版“先降理解成本、暂不改底层逻辑”的 PM/UX 讨论方向：短期优先把命名与解释层做诚实（如把“最近 7 天”校正为更贴近 upcoming 的表达、在日历/时间线显式标出“当前工作区 / 已排期 / 当前窗口”三层范围），并补“当前工作区总数 / 已排期 / 当前窗口可见 / 未排期”这类可解释计数与未排期兜底入口；中期再评估是否把 `dueAt` 的“计划完成”和“硬截止”拆成更清楚的时间语义。
- 已正式落地 B 方案到 `web/src/App.tsx` 与 `web/src/index.css`：系统视图“最近 7 天”更名为“未来 7 天”，并在日历/时间线视图前新增可见解释层，明确展示“当前工作区 / 投影视图 / 时间窗口”范围芯片，以及“当前工作区 / 已排期 / 当前窗口 / 未排期 / 窗口外”数量拆账；同时补上未排期任务的详情直达入口、窗口外任务的预览列表与“跳到最近安排”回路，前端构建已通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build`。
- 已继续把 B 方案收口为统一顶部统计模块：`web/src/App.tsx` 将原顶部独立计数条移除，改为让 `ProjectionSummary` 成为所有工作区视图共用的唯一统计容器；其中日历/时间线继续展示 projection 口径与未排期/窗口外回路，列表/看板/四象限则共用同一块顶部统计并把搜索框、新手指引入口整合进模块头部，同时去掉时间线/四象限内部重复摘要；`web/src/index.css` 同步补齐统一头部工具区与响应式布局，构建与 lint 均已通过。
- 已按最新反馈继续做 A+B 收口：移除工作区顶部重复解释与独立标签筛选条，把“清空标签”并入统一统计模块工具区；同时把统一模块文案改短（如“工作区 / 视图 / 窗口”芯片、列表/看板/四象限默认不再写长说明、日历/时间线只保留一句短解释），并把 padding / gap / 指标卡高度与字体层级整体压缩，让主任务视图露出更多，最新前端构建已再次通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build`。
- 已继续收口顶部信息层：`web/src/App.tsx` 去掉原有标题栏文案区，仅在窄屏/抽屉模式下保留无标题的紧凑控制条，避免与统一统计模块标题重复；`web/src/index.css` 同步删掉废弃标题栏样式并压缩顶部操作条，前端构建再次通过。另对“硬性 ddl + 计划完成时间”给出产品建议：优先采用双时间语义（Deadline / Planned Finish）而不是继续复用单一截止时间，并在列表卡片、详情面板、时间视图里明确区分“不可违背的外部约束”与“内部计划落点”，后续如推进实现可先从详情编辑与排序/筛选语义切入。
- 已确认双时间语义的筛选收敛原则：不为“计划完成 / 硬性 DDL”各自复制一整套新视图，而是让同类筛选共用同一行入口，默认落在“计划完成”，用户需要时再一键切换到“硬性 DDL”；该原则后续应同步约束 Today / Upcoming / overdue / risk 等同类时间筛选设计，避免导航膨胀。
- 已继续推进双时间语义的 PM/UX 设计细化：master 接受“同类筛选共用一行、默认计划完成、按需切换到硬性 DDL”的方向，后续详情面板、列表卡片与时间视图都应围绕“计划完成是执行主时间、硬性 DDL 是风险约束时间”展开，不再回到双入口膨胀方案。
- 已完成双时间语义第一批代码落地并收口通过本地 lint：`web/src/App.tsx` 补齐 `TaskTimeSummary`、今日/未来 7 天同一行内“计划/DDL”切换、详情面板的“开始时间/计划完成/硬性 DDL”三段编辑，以及列表/看板/四象限卡片的计划时间主展示 + DDL 风险 badge；`web/src/utils/desktop-repository.ts` 同步把 workspace shell 输入类型补齐 `selectionTimeModes`，并将桌面统计逾期口径与 attentionOnly 提醒候选切到“DDL 优先、计划回退”；`web/src/index.css` 补齐导航切换、双时间 badge 与详情警示样式。本轮尚未进入日历/时间线 DDL marker、提醒引擎文案与 build/UX focused 复查。

