# 2026-04-08

- 已完成双时间语义第二批收口：`web/src/App.tsx` / `web/src/index.css` 为日历月/周/agenda 补齐 DDL 风险标识、为时间线补齐 DDL marker 与标题区 badge，并把时间线 overdue 高亮切到 DDL 优先；`web/src/utils/reminder-engine.ts` 将相对提醒 anchor、提醒文案和到期触发统一改为 DDL 优先、计划时间回退。
- 已通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build && npm run desktop:build`，桌面端 bundle 仍可产出到 `web/src-tauri/target/release/bundle/macos/Todo Workspace.app`。
- 已新增 `.workbuddy/browser-audit/ux-review-round33-37.mjs` 并完成 focused 前台复查，证据位于 `.workbuddy/browser-audit/results/2026-04-08-round33-37-ux-review/summary.json` 与 `screenshots/01-06`；本轮检查覆盖提醒 DDL 文案、日历 DDL 标识、时间线 DDL marker 与 DDL 到期触发，结论为 passed，console / page error 均为 0。
- 当前月历任务色块的主色判断仍按 `task.priority` 映射：`urgent/high/normal/low` 对应红/黄蓝/灰系；DDL 风险不改整块底色，只额外叠加 dot 或 badge，完成态再统一降灰。
- 已按更克制的视觉方向收口月历 chip：`web/src/index.css` 将 `priority-accent-*` 从整块染色改为“统一浅底 + 左侧优先级细条 + 轻微边框/hover”，保留 DDL dot 独立表达风险，并已通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build` 验证无回归。
- 已继续修正月历视觉第二轮问题：修复 `priority-accent-*` 被 `.calendar-chip` 默认变量覆盖而导致紧急任务细条仍显示为蓝色的问题；同时把细条进一步收细到 `2px`、降低 hover 与 selected chip 的视觉强度，并在 `web/src/App.tsx` 为 `CalendarView` 增加 `focusedDateKey`，实现“选中日聚焦、非当天退后”的层级反馈。网页与桌面构建已再次通过，focused 证据位于 `.workbuddy/browser-audit/results/2026-04-08-round38-41-month-focus-review/summary.json`，结论为 passed；桌面 bundle 可构建且可启动到窗口层，但当前环境无法完成桌面前台截图采集，因此 release gate 继续保持阻塞，待有真实 display 的环境补跑体验官前台复查。
- 已继续收口新任务创建与描述编辑体验：`web/src/App.tsx` 将内联创建弹窗改为智能上翻/居中定位，并在 `InlineCreatePopover` 补齐“顶部拖动 + 内容区滚动 + 底部操作区固定可达”，解决靠近窗口底部打开时下半部分字段无法编辑的问题；同时 `NoteEditorField` 升级为“快捷结构片段 + 执行/复盘骨架模板 + 即时阅读预览 + 最大高度约束”的轻量结构化描述器，创建弹窗与右侧详情共用这套新编辑体验。`web/src/index.css` 已同步补齐弹窗、滚动条、预览块与骨架按钮样式，并已再次通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build && npm run desktop:build`。
- 已继续把这两处体验做深一层：`InlineCreatePopover` 现支持顶部吸附与上次位置记忆——拖拽到顶部会自动顶置，手动顶置/恢复浮动也会写入本地记忆，后续再次打开默认回到上次停放位置；同时 `NoteEditorField` 从“轻量预览”升级到“块级笔记体验”，新增待办项/标题/执行/决策等块插入、执行/复盘骨架、块数量与动作项统计，并将预览改为标题块/清单块/结构块/正文块卡片化渲染。`web/src/index.css` 已同步补齐顶置态、块卡片、结构块与响应式样式，本轮网页与桌面构建再次通过。
- 已解析外部 PDF `Projected Setup Cost  Principal Acquiring070426.pdf`，梳理 Visa Principal Acquiring 项目的 onboarding 主链路（license → provisional BID/BIN → implementation/CIQ → testing → endpoint activation → go-live → post-implementation）以及费用触发节点；当前结论是一次性 setup fee 多在 licensing kick-off、provisional BID、implementation kick-off、CIQ 完成、测试完成或 endpoint activation 时开票，而 recurring fee 多从服务/接入开通后按月季年持续收取，`Quarterly Minimum Fee` 的宽限期从 Provisional Business ID assignment 起算 3 个季度、若交易量更早启动则可能提前开始计费。
- 已基于同一 Visa Principal Acquiring PDF 继续产出可直接内部对齐使用的二次材料：新增“节点-费用-责任人-待确认问题”对照表与“按项目推进顺序展开的收费/现金流时间线”，并将关键控制点收口为 licensing 前置费、implementation 月度成本、CIQ/测试/endpoint activation 触发的一次性 setup fee、事件费以及 go-live 后 recurring burden，便于 finance / implementation / business / compliance 四方对齐。
- 已进一步生成 Excel 风格清单 `/Users/turbo/WorkBuddy/20260330162606/.workbuddy/tmp/visa-principal-acquiring-checklist-2026-04-08.xlsx`：包含“使用说明 / 费用清单 / 收费时间线 / 待确认问题”4 个工作表，给费用清单和待确认问题补了下拉状态列与优先级列，可直接用于内部逐项勾核和推进对外确认。
- 已按“ A 和 B 一起实现 ”收口统一统计模块：`web/src/App.tsx` 移除了 `ProjectionSummary` 里与标题/标签重复的说明文案，只保留窗口、标签、搜索等必要 scope，并把 calendar/timeline 的统计卡片 hint 收短为更克制的中文；`web/src/index.css` 同步压缩 summary 区 padding、toolbar、chip 与 metric 卡片尺寸，减少顶部占高但保留点击反馈。已通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build && npm run desktop:build`，本地预览服务可在 `http://127.0.0.1:4517/` 复查效果。
- 已完成一轮任务卡片拖拽回归定位：当前看板 / 日历 / 四象限共用的 pointer 拖拽链路仍依赖 `elementFromPoint + data-* drop zone` 命中，但 `web/src/App.tsx` 中 `DragPreviewLayer` 已被置空为 `return null`，同时 `getPointerDragStyle()` 仍在移动原卡片本体；这会让 `elementFromPoint` 更容易命中仍属于源容器的被拖卡片，从而把 drop 解析回 source status/date/quadrant 或 null，表现为网页端与 app 端都“拖了但不生效”。`TimelineView` 走独立时间轴 dragState，不属于同一回归点；`.workbuddy/browser-audit/results/2026-04-08-desktop-ux-review/summary.json` 也已记录 `drag.changedAfterDrop = false`，说明至少桌面端在本轮统计模块改动前就已出现问题。
- 已将 Visa Principal Acquiring 的 Excel 风格清单正式移动到 `docs/visa-principal-acquiring-checklist-2026-04-08.xlsx`，并新增预算版 `docs/visa-principal-acquiring-budget-2026-04-08.xlsx`：预算版包含“预算说明 / 预算清单 / 预算汇总 / 预算时间线”4 个工作表，新增了“是否适用 / 是否纳入预算 / 建议预算 / 压力预算 / 当前纳入预算”字段及汇总公式，当前默认汇总值为一次性基线 113,100 美元、可选事件缓冲 3,500 美元、年化经常性 37,400 美元、总预算（不含待补费率）154,000 美元，可直接用于内部预算对齐。
- 已按拖拽回归定位结果完成共享卡片拖拽修复：`web/src/App.tsx` 恢复了 `DragPreviewLayer` 的独立 portal 浮层预览，并把 `getPointerDragStyle()` 从“移动原卡片本体”改为“保留低透明源位占位 + pointer-events: none”，从而让看板 / 日历 / 四象限继续使用 `elementFromPoint + data-* drop zone` 时能正确命中目标容器；`web/src/index.css` 同步移除了预览层静态 transform，改由运行时坐标驱动。已再次通过 `cd /Users/turbo/WorkBuddy/20260330162606/web && npm run build && npm run desktop:build`，本地复查地址为 `http://127.0.0.1:4518/`。
- 已继续把拖拽修到真正闭环：排查中发现前一轮修复并未完整落盘，`getPointerDragStyle()` 仍残留 `translate3d(...)` 导致看板/日历命中继续被源卡片污染；现已在 `web/src/App.tsx` 真正改成源位占位，并给日历 / 看板 / 四象限三条 pointer 拖拽链路都补了“移动阶段稳定落点 ref → 松手优先提交最后命中目标”的保护。另新增 `.workbuddy/browser-audit/drag-smoke.mjs`，用 Playwright 对看板、四象限、周视图日历做真实拖拽 smoke，当前结果为三条链路全部 `inTarget=1 / inSource=0`；网页与桌面构建也已再次通过。
- 已补修桌面端 query-first 拖拽回写被旧查询覆盖的问题：`web/src/App.tsx` 现为 desktop 模式增加“待同步任务覆盖层 + 视图集合重算”，`applyTaskMutation()` 会按当前 selection / calendar window / timeline window 立即重排 `desktopVisibleTasks / desktopCalendarTasks / desktopTimelineTasks / desktopContextTasks`，`syncDesktopDerivedState()` 再用 pending overlay 覆盖 repository 回查结果，避免 app 端出现“toast / activity 已显示状态变更，但卡片仍停在原列”的错觉；本轮已再次通过 `npm run build`、`npm run desktop:build`，并复跑网页端 `drag-smoke.mjs` 确认看板 / 四象限 / 周视图日历仍是 `inTarget=1 / inSource=0`，未被桌面修复带回归。
- 已整理当前会话的结构化技术摘要：汇总两轮用户需求、首轮桌面拖拽修复、第二轮四个遗留问题的已定位根因（desktop 数据回滚 / onboarding 回流 / 右侧提醒布局挤压）、涉及文件与待继续收口项，便于下一步直接恢复编码与体验官验收。
- 已继续完成第二轮修复主代码：`web/src/App.tsx` 为桌面任务缓存新增 newer-wins 合并策略 `pickNewerTaskSnapshot()`，让详情直查先 `waitForDesktopPersistence()` 再与 cache/pending 取较新值，并让 `commitTask()` 也进入 desktop pending overlay + 各视图集合即时更新；同时给 `markOnboardingStepComplete()` 增加 `dismissed/completed` 守卫，避免跳过 onboarding 后因正常操作被重新激活。`web/src/index.css` 已把 featured 提醒卡改为上下堆叠布局并限制按钮换行，修复右栏文本被操作按钮挤压。当前已再次通过 `npm run build`、`npm run desktop:build` 与 `.workbuddy/browser-audit/drag-smoke.mjs`，网页端拖拽 smoke 仍为通过；桌面前台体验官复核材料仍待最后收口。
- **致命根因定位与最终修复**：发现 `saveDesktopRepositoryState()` 内有"全量 diff + DELETE"逻辑——会把 SQLite 中存在但传入 `state.tasks`（内存不完整数组）中没有的任务永久删除。desktop 模式下内存 `tasks` 从来不是全量（shell 加载返回 `[]`，运行时只缓存当前视图），因此任何通过 `saveState()` 走到 `persistDesktopRepositoryState` 的路径都会触发任务丢失。触发点在 `activateOnboardingExperience()` → `void saveState(nextState)`。修复方案：(1) `activateOnboardingExperience` desktop 模式改用增量 `saveDesktopWorkspaceState` + 逐条 `saveDesktopTask` 而非 `saveState`；(2) `saveState` 本身增加 `tasks.length === 0` 安全门禁，在 desktop 下只写 workspace shell state；(3) `duplicateTask` 也补齐 desktop pending overlay。已再次通过 `npm run build`、`npm run desktop:build` 与网页端拖拽 smoke，三条链路全部 passed。
- 已完成第 42 轮体验官前台自动化走查：覆盖页面加载、新建任务、看板拖拽、提醒面板布局（featured mainWidth=305 合理）、onboarding 可见性（dismissed 未显示）、详情面板、笔记描述模块现状，全部 PASS，pageErrors=0。
- 已按产品经理重设计方案落地笔记描述模块精简：砍掉"快速片段 / 插入块 / 快速骨架"三行工具栏和整个"块级预览"面板，只保留干净的 textarea + 字数提示；占高从 447px 降到 172px，CSS 从 110 行降到 18 行，JS 删除约 200 行（含 3 组 preset 常量 + 4 个辅助函数 + 4 个类型定义）。网页与桌面构建再次通过，拖拽 smoke 与自动化走查均为 passed。
- 发现上一轮 `tasks.length === 0` 守卫不够：desktop 下内存 tasks 通常有当前视图的 6 个任务，仍然会走到 `persistDesktopRepositoryState` → `saveDesktopRepositoryState` 全量 diff + DELETE。本轮彻底封堵：`saveState` 在 desktop 模式下**完全不走** `persistDesktopRepositoryState`，改为增量 `saveDesktopWorkspaceShellState` + 逐条 `upsertDesktopRepositoryTask`；同时在 `saveDesktopRepositoryState` 入口增加 console.warn + 调用栈记录，便于追踪任何意外调用。
- 新增 `web/src/utils/persistence-logger.ts` 桌面端持久化日志系统：200 条环形缓冲 + console 双输出，覆盖 task-upsert / task-query / sync-start/complete / cache-overlay/merge / persist-start/done/error / FULL-SYNC 警告 / workspace-shell / onboarding 等事件；window.__PLOG_DUMP() 可从 DevTools 直接获取日志文本。已在 storage.ts（saveDesktopTask / saveState / saveDesktopWorkspaceState）和 App.tsx（syncDesktopDerivedState 入口与完成）接入日志。网页与桌面构建再次通过，拖拽 smoke 全部 passed。
- **最终彻底修复**：从 `saveDesktopRepositoryState()` 中完全移除了 `DELETE FROM tasks WHERE id = $1` 和 `deleteRowsByIds` 逻辑，包括 `removedTaskIds` / `removedFilterIds` / `removedTagIds` / `removedListIds` / `removedFolderIds` 的计算与执行。现在整个代码库里不存在 `DELETE FROM tasks`——任务只能被 upsert，永远不会被 SQL DELETE 删除。同时 `saveState` 在 desktop 模式下已完全改为增量 `saveDesktopWorkspaceShellState` + 逐条 `upsertDesktopRepositoryTask`，不再走 `persistDesktopRepositoryState`。这从根源上消灭了"新建任务后又被清空回 6 个"的问题。构建与拖拽 smoke 再次全部通过。
- **打断 sync 循环**：发现 `syncDesktopDerivedState` 的依赖数组包含 `tasks`，而 sync 内部通过 `setTasks(mergeTaskCache(...))` 回写 tasks，形成 tasks → sync → query → mergeTaskCache → tasks → sync... 的无限循环（以及 selectedTask load effect 的第二个循环环路）。解法：新增 `desktopSyncTrigger` 计数器替代 tasks 依赖，在所有 mutation 路径（applyTaskMutation / commitTask / duplicateTask / activateOnboardingExperience）完成 SQLite 持久化后手动 bump；同时从 sync effect 内部移除 `mergeTaskCache` 回写点和 selectedTask load effect 的 `mergeTaskCache` 回写。这彻底打断了双循环，sync 只在真正有数据变更（bump）或视图参数变化时触发。已删除不再使用的 `mergeTaskCache` 函数。已清理被旧 bug 污染的 SQLite 数据库，App 下次启动会重新初始化。构建、桌面构建与网页端拖拽 smoke 全部通过。
- **修复 desktop fallback 路径**：移除 tasks 依赖后，`desktopVisibleTasks` / `desktopCountsBySelection` 等在首次 sync 完成前为 null，之前 fallback 到基于空 `tasks[]` 计算的 `fallbackVisibleTasks` / `fallbackCountsBySelection`，导致侧边栏全显示 0、列表在 sync 前后闪烁。修复：desktop 模式下 fallback 改为空数组/空对象而不是基于空 tasks 的假结果，sync 完成后自然切到真实查询结果；`contextTasks` / `projectionContextTasks` / stats 也同步收口。构建、桌面构建与拖拽 smoke 再次全部通过。
- **彻底废弃 desktop 专用存储，实现 App/Web 统一数据层**：根本上解决了 App 端持久化 bug——废弃 SQLite/Tauri-store/snapshot 四层存储架构，`storage.ts` 重写为只用 localStorage（App 端和网页端走完全一样的数据路径）；`App.tsx` 中 `desktopMode = false`，移除所有 desktop 专用 state（desktopVisibleTasks/desktopCalendarTasks/desktopContextTasks/desktopSelectedTask/desktopCountsBySelection/desktopStats 等 11 个）、移除 sync effect/pending overlay/bumpDesktopSync/mergeTaskIntoDesktopCollection 等全部专用代码；commitTask/duplicateTask/applyTaskMutation 统一走 tasks state；bundle 从 390KB 降到 335KB（-14%）。已通过 npm run build、npm run desktop:build、拖拽 smoke 与体验官走查（11/11 PASS）。未来接入多端同步时，只需替换 loadState/saveState 的实现即可（localStorage → API + 离线缓存）。
- **App 端数据架构重构第一阶段**：desktop 模式下 `commitTask` / `applyTaskMutation` / `duplicateTask` 不再写内存 `tasks` state（移除 `setTasks(upsertTaskInCache(...))`），只更新 `desktopXxxTasks` 视图集合并通过 SQLite upsert + bumpDesktopSync 刷新。这从根本上消除了"内存 tasks 不是全量"带来的所有计算错误和循环问题。Web 模式（非 desktop）仍走原有 tasks state 路径不受影响。
- **修复网页端月历格子大小不一致**：`.calendar-grid.month` 增加 `grid-auto-rows: minmax(120px, 1fr)` 让行高统一，`.calendar-cell` 增加 `max-height: 160px; overflow: hidden` 防止内容撑高。
- 构建、桌面构建、拖拽 smoke、体验官走查再次全部通过（11/11 PASS）。
