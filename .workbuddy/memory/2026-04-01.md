# 2026-04-01 工作日志

- 按用户反馈完成一轮产品细化：把日历/看板/时间线/四象限里的直接创建入口收敛为更克制的图标化交互，减少显性提示语。
- 重做任务卡片里的优先级与标签层级：优先级改为中性短码徽标配色点，标签改为更轻的色块芯片，避免颜色语义混淆。
- 修正智能识别创建逻辑：快速创建与内联创建在识别日期时间时保留完整原始输入作为任务标题，不再截断时间文本。
- 体验走查中顺手补齐了 agenda / timeline / kanban / matrix 新建入口缺失的样式，并让看板/四象限日期展示回退到 `dueAt ?? startAt`，构建通过，继续使用 `http://127.0.0.1:4173/` 作为本地预览地址。
- 用户要求以后称呼其为 master、助手名称固定为贾维斯；本轮继续深化 parity todo：看板已支持原生拖拽改状态，时间线已支持拖动/拉伸任务条改开始与结束时间。
- 重做看板与四象限里的状态表达：补充显式状态徽标、选中态、完成态与拖拽反馈，避免待办/进行中/已完成仍靠猜测区分。
- 修复真实提醒链路：到期提醒与相对/绝对提醒进入统一分发逻辑，新增右栏提醒中心；番茄回合结束会触发可播放提示音，并在允许时发送浏览器通知。
- 项目团队补产《2026-04-01 产品团队第 10 轮迭代与 UX 深度复审总结》，把本轮 10 轮打磨与体验官专业建议沉淀到 `docs/pm/2026-04-01-round10-ux-iteration-summary.md`。
- 第 11 轮继续收口工作台体验：时间线新增日/周双尺度与独立导航，提醒中心支持 10 分钟/1 小时稍后提醒，看板拖拽补了可撤销微反馈，并修复番茄倒计时剩余时间计算和刷新恢复逻辑；本轮总结已写入 `docs/pm/2026-04-01-round11-ux-iteration-summary.md`，构建通过。
- 第 12 轮按最新反馈继续收口：日历月/周视图都支持把任务直接拖到其他日期改期；番茄专注与习惯入口、视图和持久化状态已下线；右栏改成顶部压缩提醒摘要 + 下方主任务详情，提醒只突出最近一条并支持稍后/收起/清空；本轮总结写入 `docs/pm/2026-04-01-round12-ux-iteration-summary.md`，构建通过。
- 排查了 WorkBuddy 工作区目录命名问题：当前可见配置里未发现“新建目录自动带描述后缀”的现成开关；历史目录重命名会牵涉项目内明文引用、WorkBuddy 的 `storage.json`、`workspaceStorage/*/workspace.json` 以及大量 `User/History/*/entries.json`，已形成审计报告 `workbuddy-folder-naming-audit-2026-04-01.md`，建议先做映射表再单目录试点。
- 继续收口待办工具细节体验：日历导航新增“显示/隐藏已完成”开关，月/周/列表三种日历模式都可按需展示已完成任务并统一使用置灰+中划线；任务详情右上角和看板卡片左上角的状态徽标已支持直接切换状态，并统一走状态反馈与撤销链路；状态文案收敛为“待办 / 进行中 / 已完成”，同时把看板底部冗余状态控件换成更有用的子任务进度信息，本轮构建通过并可在本地预览验证。
- 评估了把当前 Vite + React + TypeScript + localStorage 的 Web 待办项目落地为 macOS 独立 App 的路线：推荐优先采用 Tauri 2 封装现有前端，逐步把浏览器 localStorage 迁移到应用沙盒内的文件型存储或 SQLite，并补齐原生通知、声音、Dock 徽标、开机启动、签名与 notarization 这一整套桌面化能力。
- 已在 `web/` 内完成 Tauri 2 首版桌面壳接入：新增 `src-tauri/`、桌面脚本、固定端口 Vite 配置和一套可打包图标资源，`npm run build` 与 `npm run tauri info` 均通过；当前唯一实质阻塞是本机尚未安装 `rustup/rustc/cargo`，待补齐后即可继续实际拉起 macOS 桌面 App。
- 已继续推进 macOS 桌面化：完成 Rust 工具链安装，并成功通过 `npm run desktop:build` 产出 `/web/src-tauri/target/release/bundle/macos/Todo Workspace.app`；同时把 `tauri.conf.json` 的默认 bundle 目标从 `all` 调整为 `["app"]`，避免 DMG 打包失败导致首版默认构建命令假失败。
- macOS 桌面版第二阶段已落地：为 `web/` 接入 Tauri notification/store 插件，新增 `utils/notifications.ts` 与桌面优先的 `utils/storage.ts` 双通道持久化，桌面端现已优先使用原生通知和本地 Store，并把 `firedReminderKeys` 持久化以避免应用重启后重复提醒；当前 `.app` 仍可成功构建。
- macOS 桌面版第三阶段已落地：把提醒中心从 `App.tsx` 继续拆为 `hooks/useReminderCenter.ts` 与 `utils/reminder-engine.ts`，将通知权限/声音回退/feed 管理和提醒事件计算解耦；`npm run build`、`npm run desktop:build` 与 `npm run desktop:dev` 冒烟验证均通过，当前重点已转向真机走查、SQLite 数据层和更深系统集成。
